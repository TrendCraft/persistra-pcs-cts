# PCS-CTS Environment Configuration Template
# Copy to .env and set values as needed

ANTHROPIC_API_KEY=your_key_here

# Embeddings configuration (REQUIRED for memory retrieval)
# Use tse-384 for local embeddings model (384 dimensions)
EMBED_MODEL=tse-384

# Optional execution flags (defaults shown)
LEO_POLICY_ENFORCEMENT=true
LEO_POLICY_AUDIT=true
LEO_VISION_HISTORY_MAX=0
